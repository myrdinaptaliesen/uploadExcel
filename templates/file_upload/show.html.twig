{% extends 'base.html.twig' %}

{% block title %}FileUpload{% endblock %}

{% block body %}
    <h1>FileUpload</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ file_upload.id }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ file_upload.file }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('file_upload_index') }}">back to list</a>

    <a href="{{ path('file_upload_edit', {'id': file_upload.id}) }}">edit</a>

    {{ include('file_upload/_delete_form.html.twig') }}
{% endblock %}
